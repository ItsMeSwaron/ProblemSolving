#include<bits/stdc++.h>
using namespace std;

void printer(string words)
{
    // stringstream SS(N);
    if()

}

int main()
{
    string line;
    getline(cin,line);

    // printer(line);
    stringstream SS(line);
    string words;

    printer(words);



    return 0;
}
